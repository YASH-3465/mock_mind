import { FormEvent, useState } from "react";import { Link,useNavigate } from "react-router-dom";import { toast } from "sonner";import { signup } from "../../lib/api";
export default function SignupPage(){const[name,setName]=useState("");const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[busy,setBusy]=useState(false);const navigate=useNavigate();async function submit(e:FormEvent){e.preventDefault();setBusy(true);try{await signup(name,email,password);toast.success("Account created. Log in to continue.");navigate("/login")}catch(err:any){toast.error(err?.response?.data?.detail??"Unable to create account.")}finally{setBusy(false)}}return <main className="auth-shell"><div className="auth-panel"><Link to="/" className="brand"><span className="brand-mark">M</span> MOCK MIND</Link><div className="auth-content"><div className="eyebrow">CREATE YOUR ROOM</div><h1>Build interview confidence.</h1><p>Your resume becomes the starting point for every session.</p><form onSubmit={submit}><label>Name<input value={name} onChange={e=>setName(e.target.value)} required placeholder="Your name"/></label><label>Email<input value={email} onChange={e=>setEmail(e.target.value)} type="email" required placeholder="you@example.com"/></label><label>Password<input value={password} onChange={e=>setPassword(e.target.value)} type="password" minLength={6} required placeholder="At least 6 characters"/></label><button className="primary-btn full" disabled={busy}>{busy?"Creating…":"Create account"}</button></form><p className="switch">Already have an account? <Link to="/login">Log in</Link></p></div></div><div className="auth-art"><div className="art-grid"/><div className="art-quote">Resume in.<br/><b>Interview on.</b><small>AI-generated questions • voice-first practice</small></div></div></main>}